debugger;


debugger;


va a = 1; // SyntaxError

console.log(a) // Reference 引用错误  在某一个位置使用未定义的变量

http.createSever(function(req,res) {
    req.write(asdfsdf);
    debugger;
}).listen("sdfsdf"); 


var username = "zjanm";
debugger;
username();

const fs = require("fs");
fs.readFileSync(true); //TypeError



/**
 * debugger:设置断点
 * node inspect(debug) 文件名
 * c继续执行，直到下一个断点
 * watch（"变量名"），
 * watchers查看监听的变量
 * unwatch(变量名)
  */
